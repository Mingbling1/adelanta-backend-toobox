[tool:pytest]
# Configuración específica para tests v2
addopts = --tb=short -q -p no:cacheprovider --confcutdir=.
testpaths = .
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Solo cargar conftest.py de este directorio, no del directorio raíz
confcutdir = .
